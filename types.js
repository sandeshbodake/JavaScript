// strings

const name = `i
m
sandesh
`
const name2 = 'bodake'
const name3 = "rakesh"
const winterfell = `${name} i love game of thrones`
const html = `<html><body>${winterfell}</body></html>`
document.body.innerHTML = html;


// numbers

// Math.floor, Math.ceil, Math.round
// typeOf +-infinity, NaN is `number`

// objects

var person = {
  name: 'sandesh',
  age: 22
}

// null and undefined

// Boolean and equality
